version: '2'

services:
  bot:
    build: docker/bot
    command: python -m bots.heb1025
    environment:
      BOT_TOKEN: FILLME FILLME FILLME
    restart: unless-stopped
    volumes:
      - ./heb1025bot:/app/heb1025bot
      - ./heb1025.sqlite3:/app/heb1025.sqlite3
